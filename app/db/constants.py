"""Shared DB constants (must match embedding model output size for pgvector column)."""

# text-embedding-3-small default dimensions; mock embeddings use the same size for storage.
VECTOR_STORAGE_DIMENSIONS = 1536
